{
  "_note_auto_discovered_plugins": "Firebase, GitHub, Context7, Playwright, Serena, and Episodic Memory MCP tools are auto-discovered by Claude Code's plugin system and do NOT need entries here. Serena plugin is disabled via settings.json.",
  "mcpServers": {
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"]
    },
    "sonarcloud": {
      "command": "node",
      "args": ["scripts/mcp/sonarcloud-server.js"],
      "env": {
        "SONAR_TOKEN": "${SONAR_TOKEN}",
        "SONAR_URL": "https://sonarcloud.io"
      }
    }
  }
}
