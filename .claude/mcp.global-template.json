{
  "$schema": "https://json.schemastore.org/mcp-servers",
  "_comment": "Global MCP server template. To use: copy or merge into ~/.claude/mcp.json",
  "mcpServers": {
    "ccusage": {
      "_comment": "Claude Code usage tracking",
      "command": "bunx",
      "args": ["@ccusage/mcp@latest"]
    },
    "playwright": {
      "_comment": "Browser automation for testing and screenshots",
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    }
  }
}
