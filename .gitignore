# dependencies
node_modules
/.pnp
.pnp.js

# testing
/coverage
.temp-test-phase-*/

# next.js
/.next/
/out/

# production
/build
dist-tests

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# build logs
*.log

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# test coverage
coverage/
.nyc_output/

# firebase
.firebase
pnpm-workspace.yaml

# data connect
src/dataconnect-generated
dataconnect/

# auto-generated pattern suggestions
scripts/suggested-patterns.json

# SonarQube/SonarCloud analysis data (large generated files - use CI artifacts instead)
docs/analysis/sonarqube-issues-*.json
.sonar/

# SonarCloud detailed reports (regenerate with: node scripts/generate-detailed-sonar-report.js)
docs/audits/sonarcloud-issues-detailed.md

# MCP configuration (VS Code - not used by Claude Code)
.vscode/mcp.json
# NOTE: .mcp.json is intentionally tracked - required for MCP persistence in cloud environments

# firebase credentials (NEVER commit!)
firebase-service-account.json
serviceAccountKey.json

# Claude Code session hook cache files
.claude/.lockfile-hash
.claude/.functions-lockfile-hash
.claude/hooks/.context-tracking-state.json
.claude/hooks/.session-state.json
.claude/hooks/.agent-trigger-state.json

# Claude Code session runtime data (per-session, not tracked)
.claude/session-activity*.jsonl
.claude/override-log*.jsonl

# Claude Code local user settings (user-specific, never commit!)
.claude/settings.local.json

# Claude Code hook backups (not tracked)
.claude/hooks/backup/

# Lighthouse audit reports (generated, not tracked)
.lighthouse/

# Audit validation state (contains local absolute paths)
docs/audits/comprehensive/validation-state.json

# Windows artifact file (created from command redirects)
nul

# Session alert state files
.claude/pending-alerts.json
.claude/hook-warnings.json
.claude/alerts-acknowledged.json
.claude/pending-mcp-save.json
.claude/hooks/.auto-save-state.json
