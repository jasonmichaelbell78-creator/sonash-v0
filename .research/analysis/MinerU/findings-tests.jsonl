{"dimension":"tests","kind":"metrics","test_file_count":1,"approx_test_function_count":1,"ci_workflow_count":5,"python_versions_tested":["3.10","3.11","3.12","3.13"],"python_versions_tested_for_actual_tests":["3.12"],"test_fixture_files":1,"conftest_files":0}
{"dimension":"tests","severity":"high","title":"Entire test suite is a single end-to-end function","file":"tests/unittest/test_e2e.py","observation":"The `tests/` tree contains exactly one pytest file (`tests/unittest/test_e2e.py`) with one test function `test_pipeline_with_two_config`. There are no unit tests, no integration tests, no separate e2e suite, no `__init__.py`, and no `conftest.py`. For a 58k-star document parsing engine with pipeline/vlm/vllm/lmdeploy/mlx/gradio backends, the breadth-to-depth ratio of testing is extremely low.","strength_or_weakness":"weakness"}
{"dimension":"tests","severity":"high","title":"Coverage threshold set to 0.2%","file":"tests/get_coverage.py","observation":"`get_coverage.py` parses `htmlcov/index.html` and asserts `percentage_float >= 0.2` — literally 0.2 percent. This is the only coverage gate in CI. Any regression that doesn't break the single e2e test passes unnoticed regardless of coverage delta. There is no Codecov upload, no per-module coverage, and no PR coverage comment.","strength_or_weakness":"weakness"}
{"dimension":"tests","severity":"high","title":"Primary test workflow only runs on tags, not PRs","file":".github/workflows/python-package.yml","observation":"`python-package.yml` triggers only on `push: tags: ['*released']` and `workflow_dispatch`. It runs `pip install -e .[core]` across Python 3.10-3.13 but never executes pytest — it only verifies installation succeeds. No pull request or push-to-main trigger exists for this workflow, so the Python matrix never validates actual test behavior.","strength_or_weakness":"weakness"}
{"dimension":"tests","severity":"high","title":"Actual test workflow runs only on dev branch push, single Python version","file":".github/workflows/cli.yml","observation":"`cli.yml` is the only workflow that runs `coverage run` (executing the single e2e test). Triggers are `push` to `master` or `dev` only — no `pull_request` trigger. Pull requests therefore never run the test suite before merge. It uses a single Python 3.12 + ubuntu-latest with no matrix, despite the project supporting 3.10-3.13 and claiming Windows/macOS/Linux compatibility (mlx/vllm/lmdeploy per-OS extras).","strength_or_weakness":"weakness"}
{"dimension":"tests","severity":"medium","title":"coverage command_line hardcodes e2e as sole target","file":"pyproject.toml","observation":"`[tool.coverage.run] command_line = \"-m pytest tests/unittest/test_e2e.py\"` hardcodes the single e2e file as the coverage entrypoint. This quirk is the mechanism by which coverage is measured: `coverage run` (with no args) reads command_line and invokes that exact pytest command. It implies e2e is the canonical and only coverage target — anything not exercised by the PDF-parse-and-compare flow is invisible to coverage.","strength_or_weakness":"weakness"}
{"dimension":"tests","severity":"medium","title":"Coverage omits user-facing CLI and server surfaces","file":"pyproject.toml","observation":"`[tool.coverage.run] omit` excludes `gradio_app.py`, `models_download.py`, `fast_api.py`, `cli/client.py`, `vlm_vllm_server.py`, `cli_parser.py`. These are the primary user entrypoints (Gradio UI, FastAPI server, CLI client, model downloader). Excluding them from coverage means the surfaces users actually touch are formally untested and not even counted against the 0.2% floor.","strength_or_weakness":"weakness"}
{"dimension":"tests","severity":"medium","title":"Fuzzy-match assertions as regression oracle","file":"tests/unittest/test_e2e.py","observation":"Regression detection relies on `fuzzywuzzy.fuzz.ratio(...) > 90` on extracted text plus substring checks (`\"$$\"`, `\"lambda\"`, `\"frac\"`) and a `len(type_set) >= 4` coverage-of-element-types check. There is no golden-file snapshot comparison, no JSON schema validation, and no per-element diff. The approach tolerates silent quality regressions as long as 90% fuzzy similarity holds for a handful of hardcoded strings from one PDF.","strength_or_weakness":"weakness"}
{"dimension":"tests","severity":"medium","title":"Single test PDF as the entire fixture corpus","file":"tests/unittest/pdfs/test.pdf","observation":"The test fixture corpus is one file: `tests/unittest/pdfs/test.pdf`. There are no DOCX, PPTX, image, multi-column, scanned, CJK-language, or malformed-input fixtures — despite the project supporting all of these formats and explicitly advertising DOCX/PPTX parsing via `python-docx`, `pypptx-with-oxml`, `mammoth`. Backend divergence (pipeline txt vs ocr vs vlm) is partially covered but only against this one English PDF about Trump and Wharton.","strength_or_weakness":"weakness"}
{"dimension":"tests","severity":"medium","title":"No conftest.py, no fixtures, no parametrization","file":"tests/","observation":"There is no `conftest.py` at any level, no `fixtures/` directory, no `@pytest.fixture` usage, and no `@pytest.mark.parametrize`. The two pipeline modes (`txt`, `ocr`) are tested via sequential hardcoded function calls inside one test. Adding a new backend or document type requires editing the monolithic test rather than adding a fixture/parameter.","strength_or_weakness":"weakness"}
{"dimension":"tests","severity":"medium","title":"No mocking of model loading, no slow/gpu markers","file":"tests/unittest/test_e2e.py","observation":"The test invokes `pipeline_doc_analyze_streaming` which loads real ML models (ONNX/layout/OCR) via `modelscope`/`huggingface-hub`. There is no mocking, no `@pytest.mark.slow`, no `@pytest.mark.gpu`, and no `skipif` for CI-without-GPU. The 240-minute CI timeout (`timeout-minutes: 240`) confirms the suite is expected to take hours. Developers without models cannot run tests locally in a reasonable timeframe.","strength_or_weakness":"weakness"}
{"dimension":"tests","severity":"medium","title":"pytest -s disables output capture globally","file":"pyproject.toml","observation":"`[tool.pytest.ini_options] addopts = \"-s --cov=mineru --cov-report html\"` hardcodes `-s` (no capture). This forces every test run to stream stdout/stderr, making CI logs noisy and preventing pytest from capturing per-test output for failure diagnosis. It also hardcodes `--cov-report html` only — no terminal report, no XML for Codecov, no lcov.","strength_or_weakness":"weakness"}
{"dimension":"tests","severity":"low","title":"Auto-rerun on failure masks flakiness","file":".github/workflows/rerun.yml","observation":"`rerun.yml` automatically re-runs failed jobs up to 3 attempts (`run_attempt < 3`) after a 600s sleep when the `ci` workflow fails on master. This silently converts flaky test failures into passes without surfacing the flakiness rate. No flake-tracking, no issue creation, no dashboard — failed-then-passed runs look identical to clean runs in the UI.","strength_or_weakness":"weakness"}
{"dimension":"tests","severity":"low","title":"Test infra helpers are brittle shell glue","file":"tests/get_coverage.py","observation":"`get_coverage.py` scrapes the coverage HTML report by parsing `htmlcov/index.html` with BeautifulSoup to find a `<span class='pc_cov'>`. This depends on an internal coverage.py HTML template that can change between versions. A simpler `coverage report --fail-under=0.2` would be one line and immune to template churn. `clean_coverage.py` has a logic bug: `if not os.path.exists(path): if os.path.isfile(path):` can never be true.","strength_or_weakness":"weakness"}
{"dimension":"tests","severity":"low","title":"Install-matrix job does validate cross-version compatibility","file":".github/workflows/python-package.yml","observation":"Despite the actual test job being single-version, the `check-install` job does run `pip install -e .[core]` across Python 3.10/3.11/3.12/3.13 with `fail-fast: false`. This at least catches dependency-resolution and syntax errors across supported Python versions, even if no tests execute. It's the only multi-version signal in CI.","strength_or_weakness":"strength"}
{"dimension":"tests","severity":"info","title":"test dependencies scoped via optional-dependencies","file":"pyproject.toml","observation":"`[project.optional-dependencies].test = [\"mineru[core]\", \"pytest\", \"pytest-cov\", \"coverage\", \"fuzzywuzzy\"]` keeps test tooling out of the default install and composes cleanly with the `core` extra. This is correct modern packaging even if the test content itself is minimal.","strength_or_weakness":"strength"}
{"dimension":"tests","kind":"summary","band":"Poor","score":18,"one_liner":"One e2e test, one PDF fixture, 0.2% coverage floor, PRs don't run tests, and a flake-retry workflow silently masks failures — the test infrastructure is essentially a smoke test dressed as a coverage system."}
