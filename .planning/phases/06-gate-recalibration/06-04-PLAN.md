<!-- prettier-ignore-start -->
**Document Version:** 1.0
**Last Updated:** 2026-03-01
**Status:** ACTIVE
<!-- prettier-ignore-end -->

---

phase: 06-gate-recalibration plan: 04 type: execute wave: 1 depends_on: []
files_modified:

- .pr_agent.toml
- docs/agent_docs/SECURITY_CHECKLIST.md autonomous: true

must_haves: truths: - "Qodo suppression rules are audited -- stale rules
removed, active rules documented" - "SECURITY_CHECKLIST items that are enforced
by ESLint are marked as such" - "No suppression rules silently block useful Qodo
feedback on current code patterns" artifacts: - path: ".pr_agent.toml" provides:
"Pruned suppression rules with stale items removed and active ones annotated"
contains: "extra_instructions" - path: "docs/agent_docs/SECURITY_CHECKLIST.md"
provides: "Enforcement status column showing which items are ESLint-enforced"
contains: "Enforced by" key_links: - from:
"docs/agent_docs/SECURITY_CHECKLIST.md" to: "eslint.config.mjs" via: "checklist
references ESLint rules that enforce security patterns" pattern: "sonash/" -
from: ".pr_agent.toml" to: "eslint-plugin-sonash/" via: "suppression rules
should not duplicate what ESLint already catches" pattern: "FP-"

---

<objective>
Audit Qodo suppression rules for staleness and sync SECURITY_CHECKLIST with ESLint enforcement reality.

Purpose: 19 Qodo suppression directives accumulated over time. Some may be stale
(the underlying issues are now caught by eslint-plugin-sonash).
SECURITY_CHECKLIST doesn't indicate which items are now auto-enforced by ESLint.
Both docs need syncing with current enforcement reality. Output: Pruned
.pr_agent.toml + SECURITY_CHECKLIST with enforcement status annotations
</objective>

<execution_context> @~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gate-recalibration/06-RESEARCH.md
@.pr_agent.toml
@docs/agent_docs/SECURITY_CHECKLIST.md
@eslint.config.mjs
@eslint-plugin-sonash/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and prune Qodo suppression rules in .pr_agent.toml</name>
  <files>.pr_agent.toml</files>
  <action>
  Audit every suppression directive in .pr_agent.toml:

1. Read all FP-\* entries in `extra_instructions` sections.

2. For each FP-\* rule, determine if it's still needed:
   - Cross-reference against eslint-plugin-sonash rules: if an ESLint rule now
     catches the same pattern, the Qodo suppression may be stale.
   - Cross-reference against recent archive files (docs/archive/REVIEWS\_\*.md)
     -- if the pattern was flagged in the last 10 PR reviews, the suppression is
     still active. Use `grep -l` to check.
   - If no recent occurrences AND ESLint now covers it, mark as stale.

3. For stale rules: remove the FP-\* line entirely from extra_instructions.

4. For active rules: add a brief comment with the date of last verification,
   e.g., `# Verified 2026-03-01 - still needed (no ESLint equivalent)`

5. Audit `[pr_reviewer.ignore]` file patterns:
   - `*.jsonl` ignore is correct (data files, not code)
   - `.claude/**` ignore is correct (tooling)
   - `*.test.ts`, `*.spec.ts` ignores: review whether Qodo should review tests.
     If tests are being ignored and we want Qodo to review test quality, remove
     these patterns. Otherwise keep.
   - `scripts/migrations/**` ignore: keep (one-off migration scripts)

6. Audit "Skip flagging" section: verify each item is still relevant.

7. Add a comment at top of the file:
   `# Last audited: 2026-03-01 (Phase 6 GATE-05)`

IMPORTANT: Be conservative. When in doubt, keep a suppression rule. Removing an
active rule causes noise in every PR. </action> <verify>Run
`node -e "const toml = require('fs').readFileSync('.pr_agent.toml','utf8'); console.log('Lines:', toml.split('\\n').length)"`
to confirm file is valid. Verify the audit comment exists at top.</verify>
<done>Qodo suppression rules audited: stale rules removed, active rules
annotated with verification date, file ignores reviewed</done> </task>

<task type="auto">
  <name>Task 2: Sync SECURITY_CHECKLIST.md with ESLint enforcement reality</name>
  <files>docs/agent_docs/SECURITY_CHECKLIST.md</files>
  <action>
  Add enforcement status to SECURITY_CHECKLIST.md (GATE-06):

1. Read the full SECURITY_CHECKLIST.md and identify each checklist item.

2. For each item, check if it's enforced by an ESLint rule in
   eslint-plugin-sonash:
   - Map checklist items to ESLint rules. Known mappings from research:
     - File I/O safety -> no-unguarded-file-read, no-stat-without-lstat,
       no-toctou-file-ops, no-non-atomic-write
     - Error sanitization -> no-raw-error-log, no-catch-console-error,
       no-unsafe-error-access
     - Shell injection -> no-shell-injection
     - innerHTML/XSS -> no-unsafe-innerhtml
     - SQL injection -> no-sql-injection
     - Hardcoded secrets -> no-hardcoded-secrets
     - Path safety -> no-path-startswith, no-empty-path-check
     - Regex safety -> no-unbounded-regex, no-unescaped-regexp-input
     - Division safety -> no-unsafe-division

3. Add an "Enforcement" column or inline annotation to each checklist item:
   - `[ESLint: sonash/no-unguarded-file-read]` for ESLint-enforced items
   - `[Semgrep: rule-name]` for Semgrep-enforced items
   - `[Manual review only]` for items with no automated enforcement

4. Add a summary section at the bottom:

   ```
   ## Enforcement Summary
   - X/Y items enforced by ESLint (eslint-plugin-sonash)
   - Z items enforced by Semgrep
   - W items require manual review only
   ```

5. Do NOT change the checklist content or structure -- only add enforcement
   annotations alongside existing items.

6. If a checklist item references security-helpers.js patterns, note both the
   helper AND any ESLint rule that enforces usage. </action> <verify>Verify the
   file has enforcement annotations by checking:
   `grep -c "ESLint:" docs/agent_docs/SECURITY_CHECKLIST.md` should return > 0.
   Check the summary section exists.</verify> <done>SECURITY_CHECKLIST has
   enforcement status on every item, summary shows coverage breakdown across
   ESLint/Semgrep/manual</done> </task>

</tasks>

<verification>
- .pr_agent.toml has audit timestamp comment at top
- Stale Qodo rules removed, active rules annotated
- SECURITY_CHECKLIST.md has enforcement annotations on checklist items
- Enforcement summary section exists with coverage counts
</verification>

<success_criteria> Qodo suppression rules pruned of stale entries.
SECURITY_CHECKLIST synced with actual ESLint enforcement -- every item annotated
with its enforcement mechanism. Both docs reflect current tooling reality.
</success_criteria>

<output>
After completion, create `.planning/phases/06-gate-recalibration/06-04-SUMMARY.md`
</output>
