<!-- prettier-ignore-start -->
**Document Version:** 1.0
**Last Updated:** 2026-03-01
**Status:** ACTIVE
<!-- prettier-ignore-end -->

---

phase: 07-integration-cutover plan: 02 type: execute wave: 1 depends_on: []
files_modified: [] autonomous: true

must_haves: truths: - "Gemini review config is version-controlled in .gemini/" -
"Compaction hooks save and restore ecosystem state" - "Cross-doc dependency
checker has no unresolved sync gaps" artifacts: - path: ".gemini/config.yaml"
provides: "Gemini review configuration" - path: ".gemini/styleguide.md"
provides: "Gemini style guide" - path: ".claude/hooks/pre-compaction-save.js"
provides: "State capture before compaction" - path:
".claude/hooks/compact-restore.js" provides: "State restore after compaction"
key_links: - from: ".claude/hooks/pre-compaction-save.js" to:
".claude/hooks/compact-restore.js" via: "shared state file format" pattern:
"compaction"

---

<objective>
Verify three independent integration requirements that may already be satisfied.

Purpose: INTG-03 (Gemini config in-repo), INTG-04 (cross-doc sync gap closure),
INTG-05 (compaction safeguards) -- research indicates these may be complete
already. This plan verifies each and documents any gaps found. Output:
Verification results for all three requirements, with fixes applied if gaps
found. </objective>

<execution_context> @~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration-cutover/07-RESEARCH.md

@.gemini/config.yaml @.gemini/styleguide.md
@.claude/hooks/pre-compaction-save.js @.claude/hooks/compact-restore.js
@scripts/check-cross-doc-deps.js </context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Gemini config and compaction safeguards</name>
  <files></files>
  <action>
Verify INTG-03 (Gemini config):
1. Confirm `.gemini/config.yaml` and `.gemini/styleguide.md` exist and are tracked by git (`git ls-files .gemini/`)
2. Confirm they contain meaningful config (not empty/placeholder)
3. If already in-repo and tracked: INTG-03 is satisfied, no changes needed

Verify INTG-05 (Compaction safeguards):

1. Read `.claude/hooks/pre-compaction-save.js` -- confirm it captures
   ecosystem-relevant state (health log path, reviews.jsonl path, warnings.jsonl
   path, or similar ecosystem v2 data paths)
2. Read `.claude/hooks/compact-restore.js` -- confirm it restores what
   pre-compaction saves
3. Check `.claude/settings.json` for hook registration (both hooks registered)
4. If compaction hooks exist and handle ecosystem state: INTG-05 is satisfied
5. If compaction hooks exist but do NOT save/restore ecosystem v2 data paths
   (e.g., `data/ecosystem-v2/*.jsonl`, `scripts/reviews/dist/`), add the missing
   paths to the save/restore lists

Document findings for each requirement. </action> <verify>

1. `git ls-files .gemini/` shows both files tracked
2. `.claude/hooks/pre-compaction-save.js` exists and handles state
3. `.claude/hooks/compact-restore.js` exists and restores state </verify>
   <done>INTG-03 verified (Gemini in-repo), INTG-05 verified (compaction
   safeguards functional)</done> </task>

<task type="auto">
  <name>Task 2: Verify cross-doc sync gap closure</name>
  <files></files>
  <action>
Verify INTG-04 (Cross-doc sync gap closure):

1. Read `scripts/check-cross-doc-deps.js` to understand its modes:
   - `--trivial` mode (pre-commit, staged files only)
   - Check if `--all` mode exists for full-repo scan
2. If `--all` mode exists, run: `node scripts/check-cross-doc-deps.js --all` and
   capture output
3. If `--all` mode does NOT exist, run the checker against key ecosystem
   documents by staging them temporarily:
   - Check the known cross-doc reference points: CLAUDE.md, ROADMAP.md,
     SESSION_CONTEXT.md, package.json, .claude/settings.json
4. Review Phase 6 summary (06-01-SUMMARY.md) for any identified sync gaps that
   were deferred
5. Manually audit: Do CLAUDE.md anti-patterns reference patterns that still
   exist in CODE_PATTERNS.md?
6. If gaps found: fix them. If no gaps: document clean result.

The cross-doc gate was recalibrated in Phase 6 (GATE-01/GATE-02) with
diffPattern filters and auto-fix mode. The verification here is confirming no
gaps remain after those fixes. </action> <verify>

1. Cross-doc checker runs without reporting sync failures
2. No stale references between CLAUDE.md, CODE_PATTERNS.md, and ROADMAP.md
   </verify> <done>INTG-04 verified -- cross-doc sync gaps resolved or
   documented</done> </task>

</tasks>

<verification>
1. INTG-03: `.gemini/` directory tracked in git with config.yaml and styleguide.md
2. INTG-04: Cross-doc checker reports no unresolved sync gaps
3. INTG-05: Compaction hooks save/restore ecosystem v2 state
</verification>

<success_criteria>

- All three verification requirements (INTG-03, INTG-04, INTG-05) confirmed
  satisfied or gaps fixed
- Each finding documented with evidence </success_criteria>

<output>
After completion, create `.planning/phases/07-integration-cutover/07-02-SUMMARY.md`
</output>
