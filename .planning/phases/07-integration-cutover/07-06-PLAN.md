<!-- prettier-ignore-start -->
**Document Version:** 1.0
**Last Updated:** 2026-03-01
**Status:** ACTIVE
<!-- prettier-ignore-end -->

---

phase: 07-integration-cutover plan: 06 type: execute wave: 3 depends_on:
["07-01", "07-02", "07-03", "07-04", "07-05"] files_modified:

- data/ecosystem-v2/ecosystem-health-log.jsonl autonomous: true

must_haves: truths: - "Composite health score baseline is documented with
per-category breakdown" - "Baseline accounts for ecosystem-controlled vs
external-feed categories" - "Score trend is tracked from pre-cutover to
post-cutover state" artifacts: - path:
"data/ecosystem-v2/ecosystem-health-log.jsonl" provides: "Baseline health score
entry" key_links: - from:
".claude/skills/ecosystem-health/scripts/run-ecosystem-health.js" to:
"data/ecosystem-v2/ecosystem-health-log.jsonl" via: "appendHealthScore
persistence" pattern: "appendHealthScore"

---

<objective>
Establish the composite health score baseline and document the final ecosystem state.

Purpose: INTG-08 -- after all integration work is complete, run a full health
check, document the per-category scores, and establish the baseline target. The
B+ (87+) target needs adjustment based on the reality that Technical Debt (F/24)
is dominated by external SonarCloud intake, not ecosystem-controlled factors.
Output: Documented baseline score with per-category analysis and adjusted
targets. </objective>

<execution_context> @~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration-cutover/07-RESEARCH.md

@scripts/health/run-health-check.js
@.claude/skills/ecosystem-health/scripts/run-ecosystem-health.js
@data/ecosystem-v2/ecosystem-health-log.jsonl </context>

<tasks>

<task type="auto">
  <name>Task 1: Run full health check and establish baseline</name>
  <files>data/ecosystem-v2/ecosystem-health-log.jsonl</files>
  <action>
Run the full ecosystem health check and document the results:

1. Run `node .claude/skills/ecosystem-health/scripts/run-ecosystem-health.js` to
   get a full health check with persistence
2. Capture the output showing composite score and per-category breakdown
3. Read the latest entry from `data/ecosystem-v2/ecosystem-health-log.jsonl` to
   get the machine-readable scores

Analyze the scores and define the baseline:

4. For each of the 8 categories, classify as:
   - **Ecosystem-controlled**: Code Quality, Learning & Patterns, Security,
     Documentation, Process & Workflow
   - **External-feed-dominated**: Technical Debt (SonarCloud bulk import),
     possibly Infrastructure
   - **Mixed**: Testing (partially ecosystem-controlled)

5. Compute an "ecosystem composite" excluding or down-weighting external-feed
   categories:
   - Option A: Exclude Technical Debt entirely, compute from remaining 7
     categories
   - Option B: Weight Technical Debt at 0.25x (acknowledging it's 75% external)
   - Choose whichever makes the B+ target achievable AND honest

6. Document the baseline in the SUMMARY with:
   - Full composite score (all categories)
   - Ecosystem composite score (adjusted)
   - Per-category breakdown with grades
   - Which categories improved since Phase 6
   - The B+ target interpretation and whether it's met

If the ecosystem composite reaches B+ (87+), the requirement is satisfied. If
not, document what specific improvements would get there and whether they're
realistic.

IMPORTANT: Do not artificially inflate scores. The baseline must be honest and
reproducible. The value is in establishing a TRACKABLE metric, not in hitting an
arbitrary number. </action> <verify>

1. `data/ecosystem-v2/ecosystem-health-log.jsonl` has a new entry with today's
   timestamp
2. Per-category scores are documented
3. Ecosystem composite is computed and documented </verify> <done>Baseline
   composite score established with per-category breakdown, ecosystem-controlled
   subset computed, B+ target assessed</done> </task>

<task type="auto">
  <name>Task 2: Final integration verification</name>
  <files></files>
  <action>
Run a comprehensive verification of all Phase 7 requirements:

1. **INTG-01**: Run session-start and confirm health:quick score appears

   ```bash
   node .claude/hooks/session-start.js 2>&1 | grep -i "health"
   ```

2. **INTG-02**: Confirm session-end SKILL.md has health:score step (7c)

3. **INTG-03**: Confirm `.gemini/` is git-tracked

   ```bash
   git ls-files .gemini/
   ```

4. **INTG-04**: Confirm cross-doc checker passes

5. **INTG-05**: Confirm compaction hooks exist and handle ecosystem state

6. **INTG-06**: Confirm v1 fallbacks exist and npm scripts updated

   ```bash
   ls scripts/*.v1.js
   ```

7. **INTG-07**: Run E2E smoke test

   ```bash
   node --test tests/e2e/pipeline-smoke.e2e.test.js
   ```

8. **INTG-08**: Confirm baseline score is persisted in health log

9. **TEST-02**: E2E test file exists and passes

10. **TEST-03**: Count test files per tier, verify 5 tiers with >= 39 total

11. **TEST-04**: Run performance budget tests

    ```bash
    node --test tests/perf/budget.perf.test.js
    ```

12. **TEST-05**: Confirm test-alongside audit was completed

Report results as a requirement checklist with pass/fail for each of the 12
requirements. </action> <verify> All 12 requirements (INTG-01 through INTG-08,
TEST-02 through TEST-05) report PASS. </verify> <done>All Phase 7 requirements
verified -- 12/12 PASS with documented evidence</done> </task>

</tasks>

<verification>
1. Full health check runs and persists baseline score
2. All 12 requirements verified with pass/fail evidence
3. Ecosystem composite score documented
4. Phase 7 is complete
</verification>

<success_criteria>

- Composite health score baseline established and documented (INTG-08)
- All 12 Phase 7 requirements pass verification
- Phase 7 is marked complete in roadmap </success_criteria>

<output>
After completion, create `.planning/phases/07-integration-cutover/07-06-SUMMARY.md`
</output>
