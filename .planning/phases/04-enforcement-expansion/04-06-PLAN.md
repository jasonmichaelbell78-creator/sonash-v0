<!-- prettier-ignore-start -->
**Document Version:** 1.0
**Last Updated:** 2026-03-01
**Status:** ACTIVE
<!-- prettier-ignore-end -->

---

phase: 04-enforcement-expansion plan: 06 type: execute wave: 2 depends_on:
[04-05] files_modified:

- data/ecosystem-v2/enforcement-manifest.jsonl
- scripts/reviews/build-enforcement-manifest.ts autonomous: true gap_closure:
  true

must_haves: truths: - "Enforcement manifest reflects actual rule-to-pattern
matches with zero inflated Semgrep entries" - "Reported automated coverage
percentage is accurate and verifiable" - "Every pattern has accurate tracked
enforcement status across all 7 mechanisms" artifacts: - path:
"data/ecosystem-v2/enforcement-manifest.jsonl" provides: "Accurate per-pattern
enforcement records" - path: "scripts/reviews/build-enforcement-manifest.ts"
provides: "Manifest builder with accurate matching" key_links: - from:
"data/ecosystem-v2/enforcement-manifest.jsonl" to: ".semgrep/rules/\*_/_.yml"
via: "semgrep mechanism field references actual rule IDs" pattern:
"active:sonash\\." - from: "scripts/reviews/verify-enforcement-manifest.ts" to:
"data/ecosystem-v2/enforcement-manifest.jsonl" via: "cross-reference
verification"

---

<objective>
Rebuild the enforcement manifest with fixed matching and verify actual coverage numbers.

Purpose: After Plan 04-05 fixed the matching logic and Semgrep refs, the
manifest must be regenerated to get accurate coverage numbers. The
VERIFICATION.md notes true coverage is likely 2-11%. If actual coverage is below
55%, assess whether additional targeted rule improvements can close the gap, or
document the true achievable coverage.

Output: Rebuilt enforcement-manifest.jsonl with accurate coverage + verified
coverage report </objective>

<execution_context> @~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enforcement-expansion/04-05-SUMMARY.md
@.planning/phases/04-enforcement-expansion/04-VERIFICATION.md
@scripts/reviews/build-enforcement-manifest.ts
@scripts/reviews/verify-enforcement-manifest.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild manifest and measure actual coverage</name>
  <files>data/ecosystem-v2/enforcement-manifest.jsonl</files>
  <action>
    Run the manifest builder to regenerate enforcement-manifest.jsonl with the fixed matching logic from Plan 04-05:

    ```bash
    npx ts-node scripts/reviews/build-enforcement-manifest.ts
    ```

    Capture the output which shows coverage stats. Record:
    - Total patterns
    - Automated count (at least one of regex/eslint/semgrep active)
    - AI-assisted count
    - Manual-only count
    - The specific automated percentage

    Then run the verifier to check for drift:
    ```bash
    npx ts-node scripts/reviews/verify-enforcement-manifest.ts
    ```

    Expected: Verifier passes (no drift between manifest active references and actual rule files).

    Record the TRUE automated coverage percentage. It will likely be significantly lower than the previous 63.1%.

  </action>
  <verify>
    Run: `npx ts-node scripts/reviews/build-enforcement-manifest.ts`
    Expected: Completes without error, produces coverage stats.

    Run: `npx ts-node scripts/reviews/verify-enforcement-manifest.ts`
    Expected: Exit code 0 (no drift detected).

    Run: `node --test tests/enforcement-manifest.test.ts`
    Expected: All 25 tests pass.

  </verify>
  <done>enforcement-manifest.jsonl is rebuilt with accurate matching. Verifier passes. True coverage percentage is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Assess coverage gap and improve if feasible</name>
  <files>
    scripts/reviews/build-enforcement-manifest.ts
    data/ecosystem-v2/enforcement-manifest.jsonl
  </files>
  <action>
    After Task 1, evaluate the actual coverage number.

    IF coverage is already at or above 55%: Document the result, no further action needed.

    IF coverage is below 55% (likely scenario): Analyze where the coverage gaps are.

    1. Look at the manifest output to identify which patterns are manual-only that COULD be covered by existing rules. The issue may be that the Semgrep ref slugs don't exactly match the pattern ID slugs due to naming differences. For example, if a Semgrep rule's code-pattern-ref slugifies to "file-reads-with-try-catch" but the pattern ID is "file-reads-with-try-catch", that is an exact match. But if the pattern ID is something slightly different, the exact match fails.

    2. Review the coverage breakdown by mechanism:
       - How many patterns are covered by regex? (check-pattern-compliance.js has 64 rules)
       - How many by ESLint? (32 rules)
       - How many by Semgrep? (20 rules with fixed refs)
       - These mechanisms may already cover many patterns via fuzzyMatch for regex/ESLint

    3. The regex rules (64 total) and ESLint rules (32 total) still use fuzzyMatch. These are the primary coverage drivers. If they contribute substantial coverage, the total may be closer to 55% than the "2-11% Semgrep-only" estimate suggests.

    4. If coverage is still below 55% after accurate measurement, consider these options:
       a. Some patterns may have near-miss slug matching for regex/ESLint. Audit the top 20 manual-only patterns and check if a regex or ESLint rule SHOULD match but the slug fuzzy match misses.
       b. If the gap is small (<10%), consider adding a few targeted regex rules to check-pattern-compliance.js for high-value manual-only patterns.
       c. If the gap is large (>20%), document the true achievable coverage and note that the 55% target may need revision given the 360-pattern count (many patterns are edge-case learnings that no automated tool would enforce).

    5. Whatever the outcome, update the manifest one final time and record the accurate number.

    IMPORTANT: Do NOT re-introduce fuzzy matching or category-level matching to inflate numbers. The goal is accurate measurement, not hitting an arbitrary target through false matches.

  </action>
  <verify>
    Run: `npx ts-node scripts/reviews/build-enforcement-manifest.ts`
    Expected: Final coverage number is printed and is accurate.

    Run: `npx ts-node scripts/reviews/verify-enforcement-manifest.ts`
    Expected: Exit 0, no drift.

    Check: No generic code-pattern-ref values reintroduced in any Semgrep YAML.

  </verify>
  <done>True enforcement coverage is measured and documented. If below 55%, the gap is analyzed with clear rationale. Manifest is accurate. ENFR-06 is either satisfied with real numbers or has documented justification for the actual achievable coverage.</done>
</task>

</tasks>

<verification>
1. enforcement-manifest.jsonl has been rebuilt with fixed matching
2. verify-enforcement-manifest.ts passes (exit 0)
3. Coverage percentage is accurate (no inflated Semgrep matches)
4. All 25 existing tests pass
5. Coverage gap is either closed or documented with rationale
</verification>

<success_criteria> The enforcement manifest reflects reality:

- Semgrep matches are exact (only rules with specific pattern refs count)
- Regex and ESLint matches are as accurate as fuzzyMatch allows (acceptable
  since rule IDs were designed for this)
- True automated coverage is measured and recorded
- ENFR-04 (accurate manifest) is fully satisfied
- ENFR-06 (coverage percentage) is either satisfied or has documented gap
  rationale </success_criteria>

<output>
After completion, create `.planning/phases/04-enforcement-expansion/04-06-SUMMARY.md`
</output>
