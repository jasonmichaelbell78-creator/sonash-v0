<!-- prettier-ignore-start -->
**Document Version:** 1.0
**Last Updated:** 2026-03-01
**Status:** ACTIVE
<!-- prettier-ignore-end -->

---

phase: 07-integration-cutover plan: 03 type: execute wave: 2 depends_on:
["07-01"] files_modified:

- .claude/hooks/session-start.js
- .claude/skills/session-end/SKILL.md
- package.json autonomous: true

must_haves: truths: - "npm run reviews:sync calls v2 pipeline, not
sync-reviews-to-jsonl.js" - "npm run consolidation:run calls v2 pipeline or is
documented as hybrid" - "v1 scripts are preserved as fallback with .v1.js
suffix" - "session-start.js references updated to v2 scripts where applicable"
artifacts: - path: "package.json" provides: "Updated npm scripts pointing to v2"
contains: "scripts/reviews" - path: "scripts/sync-reviews-to-jsonl.v1.js"
provides: "v1 fallback for review sync" - path:
"scripts/run-consolidation.v1.js" provides: "v1 fallback for consolidation"
key_links: - from: "package.json" to: "scripts/reviews/" via: "npm script
definitions" pattern: "scripts/reviews" - from: ".claude/hooks/session-start.js"
to: "scripts/reviews/" via: "execSync/execFileSync calls" pattern:
"scripts/reviews"

---

<objective>
Replace v1 scripts with v2 equivalents in session hooks and npm scripts.

Purpose: INTG-06 -- the v2 JSONL-first pipeline (built in Phases 1-3) replaces
the v1 markdown-first scripts. v1 scripts are preserved as `.v1.js` fallbacks.
Output: Updated package.json, session-start.js, and session-end SKILL.md with v2
script references. </objective>

<execution_context> @~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration-cutover/07-RESEARCH.md

@package.json @.claude/hooks/session-start.js
@.claude/skills/session-end/SKILL.md @scripts/sync-reviews-to-jsonl.js
@scripts/run-consolidation.js @scripts/check-pattern-compliance.js
@scripts/promote-patterns.js </context>

<tasks>

<task type="auto">
  <name>Task 1: Map v1 references and create v1 fallbacks</name>
  <files>scripts/sync-reviews-to-jsonl.v1.js, scripts/run-consolidation.v1.js</files>
  <action>
First, map ALL references to v1 scripts across the codebase:
```bash
grep -rn "sync-reviews-to-jsonl\|run-consolidation\|check-pattern-compliance\|promote-patterns" --include="*.js" --include="*.json" --include="*.md" --include="*.sh" .claude/ .husky/ package.json scripts/ | grep -v node_modules | grep -v dist
```

Then apply the cutover plan from research:

1. **sync-reviews-to-jsonl.js** -> v2 equivalent exists in skills/session-end
   review sync step. Copy to `scripts/sync-reviews-to-jsonl.v1.js` as fallback.

2. **run-consolidation.js** -> This is still actively used (auto-consolidation
   in session-start, consolidation in session-end). Copy to
   `scripts/run-consolidation.v1.js` as fallback. This script may NOT have a
   full v2 replacement yet -- if so, keep it as the active script and document
   that v2 consolidation is partial.

3. **promote-patterns.js** -> Research says this is already a v2 wrapper. Verify
   by reading the source. If it imports from
   `scripts/reviews/lib/promote-patterns.ts`, it's already v2. No cutover
   needed, just document.

4. **check-pattern-compliance.js** -> Used in pre-commit hook. The v2 equivalent
   is `verify-enforcement-manifest.ts` but research says "partial overlap". Do
   NOT cut over this script yet -- it's too risky for pre-commit. Document as
   "stays v1 for now, v2 coverage tracked separately."

For each v1 file being preserved:

- Copy (do not move) to `.v1.js` suffix
- Add a comment at the top of the original:
  `// v1 fallback available at: scripts/{name}.v1.js` </action> <verify>

1. `ls scripts/*.v1.js` shows fallback files created
2. `diff scripts/sync-reviews-to-jsonl.js scripts/sync-reviews-to-jsonl.v1.js`
   confirms identical
3. `diff scripts/run-consolidation.js scripts/run-consolidation.v1.js` confirms
   identical </verify> <done>v1 fallback files created for sync-reviews and
   run-consolidation; promote-patterns confirmed v2; check-pattern-compliance
   documented as staying v1</done> </task>

<task type="auto">
  <name>Task 2: Update npm scripts and session hooks to v2</name>
  <files>package.json, .claude/hooks/session-start.js, .claude/skills/session-end/SKILL.md</files>
  <action>
Update references where safe v2 replacements exist:

**package.json updates:**

- `reviews:sync`: If a v2 sync equivalent exists in `scripts/reviews/`, update
  the npm script. If not (the sync is markdown->JSONL which v2 architecture
  eliminates), add a comment or alias. Research shows the v2 architecture writes
  JSONL first, so sync becomes a backward-compat bridge. Keep the existing
  script but add a `reviews:sync:v1` alias pointing to the `.v1.js` file.
- `consolidation:run`: Similarly, add `consolidation:run:v1` alias. Keep primary
  pointing to current script if no full v2 replacement.
- `patterns:promote`: Verify it already calls v2, no change needed.
- `patterns:check`: Keep as-is (v1, used in pre-commit).

**session-start.js updates:**

- The `reviews:sync` call at line ~449 and ~491: These stay as-is since the sync
  script is still needed (it bridges markdown -> JSONL). The JSONL-first
  architecture means NEW reviews go to JSONL first, but existing markdown
  reviews still need sync.
- The `run-consolidation.js` call at line ~429: Keep as-is if no full v2
  replacement.
- Add a comment above each v1 reference:
  `// v1 script -- v2 architecture writes JSONL-first, this syncs legacy markdown`

**session-end SKILL.md updates:**

- Step 7b (Review Sync) references `npm run reviews:sync`. Keep as-is with a
  note that v2 skills write JSONL directly and this step syncs any
  manually-written markdown reviews.

The key insight: v1-to-v2 cutover for this project is NOT a full swap but a
gradual shift. The v2 pipeline handles new data (JSONL-first), while v1 scripts
handle legacy data (markdown sync). Both coexist.

Document the cutover status clearly in a comment block at the top of
session-start.js:

```javascript
// === v1/v2 Script Status ===
// sync-reviews-to-jsonl.js: v1 (bridges legacy markdown, v2 writes JSONL-first)
// run-consolidation.js: v1 (no full v2 replacement yet)
// check-pattern-compliance.js: v1 (pre-commit gate, v2 partial overlap)
// promote-patterns.js: v2 wrapper (calls scripts/reviews/lib/promote-patterns.ts)
```

  </action>
  <verify>
1. `npm run reviews:sync -- --apply` still works
2. `node scripts/run-consolidation.js --auto` still works
3. package.json has v1 fallback aliases
4. session-start.js has cutover status documentation
  </verify>
  <done>v1/v2 cutover applied where safe; coexistence documented; fallback aliases in package.json</done>
</task>

</tasks>

<verification>
1. All four v1 scripts assessed: 1 already v2, 1 stays v1, 2 have fallbacks
2. `npm run reviews:sync -- --apply` completes without error
3. `node scripts/run-consolidation.js --auto` completes without error
4. v1 fallback files exist at scripts/*.v1.js
5. session-start.js runs successfully with all v1/v2 references intact
</verification>

<success_criteria>

- v1 scripts preserved as .v1.js fallbacks (INTG-06 reversibility)
- npm scripts updated with v1 fallback aliases
- Session hooks documented with v1/v2 status
- No existing functionality broken by cutover </success_criteria>

<output>
After completion, create `.planning/phases/07-integration-cutover/07-03-SUMMARY.md`
</output>
