<!-- prettier-ignore-start -->
**Document Version:** 1.0
**Last Updated:** 2026-03-01
**Status:** ACTIVE
<!-- prettier-ignore-end -->

---

phase: 07-integration-cutover plan: 04 type: execute wave: 2 depends_on:
["07-03"] files_modified:

- tests/e2e/pipeline-smoke.e2e.test.js
- tests/integration/cross-module-pipeline.integration.test.js autonomous: true

must_haves: truths: - "E2E smoke test exercises the full pipeline: review write
-> JSONL -> consolidation -> promotion" - "Integration test verifies
cross-module data flow between review writer and consolidation" - "Both tests
pass on real project data" artifacts: - path:
"tests/e2e/pipeline-smoke.e2e.test.js" provides: "Full pipeline E2E smoke test"
min_lines: 50 - path:
"tests/integration/cross-module-pipeline.integration.test.js" provides:
"Cross-module integration test" min_lines: 40 key_links: - from:
"tests/e2e/pipeline-smoke.e2e.test.js" to: "scripts/reviews/dist/" via:
"execFileSync calling compiled v2 scripts" pattern: "scripts/reviews" - from:
"tests/e2e/pipeline-smoke.e2e.test.js" to: "scripts/health/run-health-check.js"
via: "final health check verification" pattern: "run-health-check"

---

<objective>
Create E2E smoke test and integration test for the v2 pipeline.

Purpose: INTG-07 (E2E smoke test on real data) and TEST-02 (E2E test file). Also
creates the integration tier test for TEST-03 (5-tier coverage). These tests
verify the full pipeline works end-to-end after the v1-to-v2 cutover. Output:
E2E test file and integration test file, both passing. </objective>

<execution_context> @~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration-cutover/07-RESEARCH.md

@scripts/reviews/lib/write-review-record.ts
@scripts/reviews/lib/promote-patterns.ts @scripts/reviews/lib/completeness.ts
@scripts/health/run-health-check.js @scripts/run-consolidation.js </context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E pipeline smoke test</name>
  <files>tests/e2e/pipeline-smoke.e2e.test.js</files>
  <action>
Create `tests/e2e/pipeline-smoke.e2e.test.js` using node:test (project convention).

The E2E test exercises the full pipeline on real data:

1. **Review JSONL exists**: Assert `data/ecosystem-v2/reviews.jsonl` exists and
   has >400 entries (backfilled in Phase 2)
2. **JSONL validates**: Read a sample of entries, parse as JSON, verify they
   have required fields (id, date, source)
3. **Consolidation runs**: Execute `node scripts/run-consolidation.js --auto`
   and assert it completes without error
4. **Pattern promotion runs**: Execute `node scripts/promote-patterns.js` (or
   the v2 equivalent) and assert it completes
5. **Enforcement manifest exists**: Assert
   `data/ecosystem-v2/enforcement-manifest.json` (or similar manifest file)
   exists
6. **Health check runs**: Execute
   `node scripts/health/run-health-check.js --quick` and assert it completes
   with a numeric score
7. **Gate check runs**: Execute `node scripts/check-pattern-compliance.js` and
   assert it completes (exit 0 or known exit code)

Use `execFileSync` with `{ encoding: 'utf8', stdio: 'pipe' }` for subprocess
calls.

Each step should be a separate `it()` block within a
`describe('E2E: Full pipeline smoke test')`.

Use `{ timeout: 30000 }` on the describe block since these are real operations.

IMPORTANT: This test reads REAL data files, not fixtures. It's a smoke test
verifying the pipeline works on the actual project state.

IMPORTANT: Use `const { describe, it } = require('node:test')` and
`const assert = require('node:assert')` -- project convention is node:test, not
vitest.

Create the `tests/e2e/` directory if it doesn't exist. </action> <verify> Run
`node --test tests/e2e/pipeline-smoke.e2e.test.js` and confirm all tests pass.
</verify> <done>E2E smoke test passes, exercising review JSONL -> consolidation
-> promotion -> health check pipeline</done> </task>

<task type="auto">
  <name>Task 2: Create cross-module integration test</name>
  <files>tests/integration/cross-module-pipeline.integration.test.js</files>
  <action>
Create `tests/integration/cross-module-pipeline.integration.test.js` using node:test.

This integration test verifies cross-module data flow (not just individual
scripts):

1. **Review writer output feeds consolidation**:
   - Read a review record from `data/ecosystem-v2/reviews.jsonl`
   - Verify it has the schema fields that consolidation expects (id, patterns
     array or similar)
   - This tests the data contract between write-review-record and
     run-consolidation

2. **Consolidation output feeds promotion**:
   - Verify consolidation output files exist and have expected format
   - Check that pattern data has the fields promotion expects

3. **Health checkers read ecosystem data**:
   - Run a single health checker (e.g.,
     `scripts/health/checkers/debt-health.js`) and verify it reads from expected
     data files
   - Verify the checker returns a result object with score/metrics

4. **Warning lifecycle integration**:
   - Verify `data/ecosystem-v2/warnings.jsonl` exists and can be read
   - Verify warning records have lifecycle state fields (status, created_at)

These are NOT unit tests (those exist already). They verify the DATA HANDOFF
between modules -- that output from module A is valid input for module B.

Create the `tests/integration/` directory if it doesn't exist.

Use `{ timeout: 15000 }` for the describe block. </action> <verify> Run
`node --test tests/integration/cross-module-pipeline.integration.test.js` and
confirm all tests pass. </verify> <done>Integration test passes, verifying data
contracts between review writer, consolidation, promotion, and health
modules</done> </task>

</tasks>

<verification>
1. `node --test tests/e2e/pipeline-smoke.e2e.test.js` -- all tests pass
2. `node --test tests/integration/cross-module-pipeline.integration.test.js` -- all tests pass
3. Both test files exist in their respective tier directories
4. Tests use real project data, not mocked fixtures
</verification>

<success_criteria>

- E2E smoke test exercises full pipeline end-to-end (INTG-07, TEST-02)
- Integration test verifies cross-module data contracts
- Both new test tiers (E2E, integration) have at least 1 file each (TEST-03
  progress) </success_criteria>

<output>
After completion, create `.planning/phases/07-integration-cutover/07-04-SUMMARY.md`
</output>
