<!-- prettier-ignore-start -->
**Document Version:** 1.0
**Last Updated:** 2026-03-01
**Status:** ACTIVE
<!-- prettier-ignore-end -->

---

phase: 07-integration-cutover plan: 01 type: execute wave: 1 depends_on: []
files_modified:

- .claude/hooks/session-start.js
- .claude/skills/session-end/SKILL.md autonomous: true

must_haves: truths: - "Session-start runs health:quick automatically and
displays score" - "Session-end runs full health check with score persistence" -
"Health check failures in session-start do not block startup" artifacts: - path:
".claude/hooks/session-start.js" provides: "health:quick integration as step 13"
contains: "run-health-check.js" - path: ".claude/skills/session-end/SKILL.md"
provides: "health:score step in session-end workflow" contains:
"run-ecosystem-health.js" key_links: - from: ".claude/hooks/session-start.js"
to: "scripts/health/run-health-check.js" via: "execFileSync with --quick flag"
pattern: "run-health-check.\*--quick" - from:
".claude/skills/session-end/SKILL.md" to:
".claude/skills/ecosystem-health/scripts/run-ecosystem-health.js" via: "node
command in skill step" pattern: "run-ecosystem-health"

---

<objective>
Wire health checks into the session lifecycle so they run automatically.

Purpose: INTG-01 and INTG-02 -- session-start shows a quick health score on
every startup; session-end persists a full health score for trend tracking. No
manual invocation needed. Output: Updated session-start.js hook and session-end
SKILL.md with health check steps. </objective>

<execution_context> @~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration-cutover/07-RESEARCH.md

@.claude/hooks/session-start.js @.claude/skills/session-end/SKILL.md
@.claude/skills/ecosystem-health/scripts/run-ecosystem-health.js
@scripts/health/run-health-check.js </context>

<tasks>

<task type="auto">
  <name>Task 1: Add health:quick to session-start.js</name>
  <files>.claude/hooks/session-start.js</files>
  <action>
Add a new step after the TDMS health check block (after line ~565, before the final summary output at line ~567). This is step 13: Health Quick Check.

Implementation:

1. Add a try/catch block that runs `scripts/health/run-health-check.js --quick`
   via execFileSync
2. Use `{ cwd: projectDir, encoding: 'utf8', stdio: 'pipe', timeout: 10000 }`
   options
3. Parse the output for the score line (look for "Score:" pattern)
4. Print score as `Health: {score} (quick)` if found
5. On error, print `Health: skipped (non-fatal)` -- do NOT increment warnings
   counter
6. This must be NON-BLOCKING: errors must not prevent session startup

IMPORTANT: Use `run-health-check.js` (the raw CLI runner), NOT
`run-ecosystem-health.js` (the skill orchestrator that persists). Session-start
should only display, not persist, to keep it fast.

IMPORTANT: The quick check runs in 0.13s. The 10s timeout is a safety net only.

Pattern to follow (matches existing session-start.js style):

```javascript
// Step 13: Health quick check (non-blocking)
try {
  const healthOutput = execFileSync(
    "node",
    ["scripts/health/run-health-check.js", "--quick"],
    {
      cwd: projectDir,
      encoding: "utf8",
      stdio: "pipe",
      timeout: 10000,
    }
  );
  const scoreLine = healthOutput.split("\n").find((l) => l.includes("Score:"));
  if (scoreLine) {
    console.log(`Health: ${scoreLine.trim()}`);
  }
} catch {
  console.log("Health: skipped (non-fatal)");
}
```

  </action>
  <verify>
Run `node .claude/hooks/session-start.js` and confirm:
1. It completes without error
2. A "Health:" line appears in output near the end
3. Total runtime is still under 30s
  </verify>
  <done>session-start.js includes health:quick step that displays score without blocking startup</done>
</task>

<task type="auto">
  <name>Task 2: Add health:score step to session-end SKILL.md</name>
  <files>.claude/skills/session-end/SKILL.md</files>
  <action>
Add a new step "7c. Health Score Snapshot" to the session-end skill, between step 7b (Review Sync) and step 8 (TDMS Consolidation).

Add this markdown section:

```markdown
## 7c. Health Score Snapshot

Run a full ecosystem health check and persist the score for trend tracking:

\`\`\`bash node .claude/skills/ecosystem-health/scripts/run-ecosystem-health.js
\`\`\`

This runs all 10 health checkers, computes the composite score, persists to
`data/ecosystem-v2/ecosystem-health-log.jsonl`, and outputs a dashboard summary.
If the score has degraded since last check, note it in the session summary (Step
3b).

**Do not skip this step** -- it provides the health trend data that
session-start references.
```

IMPORTANT: Use `run-ecosystem-health.js` (the skill orchestrator) NOT
`run-health-check.js` (the raw runner). The orchestrator handles persistence to
the health log and trend computation.

Also update the Version History table at the bottom to add a new row: | 1.1 |
2026-03-01 | Add health score snapshot step (INTG-02) | </action> <verify> Read
.claude/skills/session-end/SKILL.md and confirm:

1. Section 7c exists between 7b and 8
2. It references run-ecosystem-health.js (not run-health-check.js)
3. Version history updated </verify> <done>session-end SKILL.md includes
   health:score step that persists full check results</done> </task>

</tasks>

<verification>
1. `node .claude/hooks/session-start.js` completes and shows Health score line
2. `.claude/skills/session-end/SKILL.md` contains "7c. Health Score Snapshot" section
3. Session-start uses --quick flag (fast, no persistence)
4. Session-end uses run-ecosystem-health.js (full, with persistence)
</verification>

<success_criteria>

- Session-start displays health:quick score automatically (INTG-01)
- Session-end includes health:score persistence step (INTG-02)
- Neither addition blocks or breaks existing session lifecycle
  </success_criteria>

<output>
After completion, create `.planning/phases/07-integration-cutover/07-01-SUMMARY.md`
</output>
