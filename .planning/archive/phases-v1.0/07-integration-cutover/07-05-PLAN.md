<!-- prettier-ignore-start -->
**Document Version:** 1.0
**Last Updated:** 2026-03-01
**Status:** ACTIVE
<!-- prettier-ignore-end -->

---

phase: 07-integration-cutover plan: 05 type: execute wave: 2 depends_on:
["07-03"] files_modified:

- tests/perf/budget.perf.test.js autonomous: true

must_haves: truths: - "Performance test verifies gate check completes in <3s" -
"Performance test verifies health:quick completes in <1s" - "All 5 test tiers
have at least 1 file" - "Every script in scripts/reviews/ and scripts/health/
has a corresponding test file" artifacts: - path:
"tests/perf/budget.perf.test.js" provides: "Performance budget assertions"
min_lines: 30 key_links: - from: "tests/perf/budget.perf.test.js" to:
"scripts/health/run-health-check.js" via: "timed execFileSync" pattern:
"run-health-check.\*--quick" - from: "tests/perf/budget.perf.test.js" to:
"scripts/check-pattern-compliance.js" via: "timed execFileSync" pattern:
"check-pattern-compliance"

---

<objective>
Create performance budget tests and audit test-alongside compliance.

Purpose: TEST-04 (performance budgets), TEST-03 (5-tier completion), TEST-05
(every script has tests). The performance tier is the last missing test tier.
The test-alongside audit ensures no untested scripts exist. Output: Performance
test file and test gap audit results. </objective>

<execution_context> @~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration-cutover/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create performance budget test</name>
  <files>tests/perf/budget.perf.test.js</files>
  <action>
Create `tests/perf/budget.perf.test.js` using node:test.

Performance budget tests (from TEST-04 requirements):

1. **Gate check (<3s)**: Time `node scripts/check-pattern-compliance.js`
   execution. Assert elapsed < 3000ms.

2. **Health:quick (<1s)**: Time
   `node scripts/health/run-health-check.js --quick` execution. Assert elapsed <
   1000ms. (Research measured 0.13s, budget is generous.)

3. **Health:full (<5s)**: Time `node scripts/health/run-health-check.js` (full
   mode). Assert elapsed < 5000ms. (Research measured 0.28s.)

4. **Consolidation (<10s)**: Time `node scripts/run-consolidation.js --auto`
   execution. Assert elapsed < 10000ms.

Each test:

- Records start time with `Date.now()`
- Runs the command via `execFileSync` with `{ encoding: 'utf8', stdio: 'pipe' }`
- Records end time
- Asserts `elapsed < budget` with descriptive message including actual time

Use `{ timeout: 30000 }` on the describe block.

Create the `tests/perf/` directory if it doesn't exist.

IMPORTANT: These run real scripts, not mocks. They measure actual performance on
the developer's machine.

Note: The pre-commit hook budget (<10s) is harder to test in isolation since
it's a shell script with many steps. Test the individual heaviest steps instead
(gate check, pattern compliance). </action> <verify> Run
`node --test tests/perf/budget.perf.test.js` and confirm all tests pass within
budgets. </verify> <done>Performance budget tests pass for gate check (<3s),
health:quick (<1s), health:full (<5s), consolidation (<10s)</done> </task>

<task type="auto">
  <name>Task 2: Audit test-alongside compliance and document tier coverage</name>
  <files></files>
  <action>
Audit TEST-05 (every script committed with tests) and TEST-03 (39 files across 5 tiers):

**TEST-05 audit:**

1. List all scripts in `scripts/reviews/lib/*.ts` and `scripts/health/*.js` and
   `scripts/health/checkers/*.js`
2. For each script, check if a corresponding test file exists in `tests/` or
   `scripts/**/__tests__/`
3. Report any scripts WITHOUT tests as gaps
4. If gaps found and they are small utility scripts, create minimal test files.
   If they are large scripts, document them as known gaps for future work.

**TEST-03 tier count:**

1. Count test files per tier:
   - Unit: `tests/**/*.test.ts` + `scripts/**/__tests__/*.test.ts` +
     `scripts/**/*.test.js` (excluding dist-tests)
   - Contract: `tests/**/contracts/*.contract.test.ts`
   - Integration: `tests/integration/*.test.js` (created in 07-04)
   - E2E: `tests/e2e/*.test.js` (created in 07-04)
   - Performance: `tests/perf/*.test.js` (created in Task 1)
2. Verify total >= 39 files across all 5 tiers
3. Verify each tier has >= 1 file
4. Document the final count

If any tier is still missing (unlikely after 07-04 and Task 1), create a minimal
test to fill it.

Output the audit results as a table showing tier, count, and sample files.
</action> <verify>

1. `find tests scripts -name "*.test.*" -not -path "*/dist-tests/*" -not -path "*/node_modules/*" | wc -l`
   shows >= 39
2. Each of the 5 tier directories has at least 1 test file
3. Any test-alongside gaps documented </verify> <done>TEST-03 verified (5 tiers
   with >= 39 files), TEST-05 audited with gaps documented</done> </task>

</tasks>

<verification>
1. `node --test tests/perf/budget.perf.test.js` -- all performance tests pass
2. Test file count >= 39 across 5 tiers
3. Each tier (unit, contract, integration, E2E, performance) has >= 1 file
4. Test-alongside gaps documented or filled
</verification>

<success_criteria>

- Performance budgets enforced via tests (TEST-04)
- 5 test tiers each populated with >= 1 file (TEST-03)
- Test-alongside compliance audited (TEST-05) </success_criteria>

<output>
After completion, create `.planning/phases/07-integration-cutover/07-05-SUMMARY.md`
</output>
