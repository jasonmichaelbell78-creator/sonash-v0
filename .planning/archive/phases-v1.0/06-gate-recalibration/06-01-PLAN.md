<!-- prettier-ignore-start -->
**Document Version:** 1.0
**Last Updated:** 2026-03-01
**Status:** ACTIVE
<!-- prettier-ignore-end -->

---

phase: 06-gate-recalibration plan: 01 type: execute wave: 1 depends_on: []
files_modified:

- scripts/config/doc-dependencies.json
- scripts/check-cross-doc-deps.js
- tests/cross-doc-deps.test.js autonomous: true

must_haves: truths: - "Cross-doc deps gate fires only on meaningful changes, not
formatting or comment edits" - "When gate detects a violation, it suggests a fix
and can auto-apply trivial fixes" - "Override rate for cross-doc deps drops
measurably from current 50% baseline" artifacts: - path:
"scripts/config/doc-dependencies.json" provides: "Recalibrated rules with
diffPattern and gitFilter on high-override rules" contains: "diffPattern" -
path: "scripts/check-cross-doc-deps.js" provides: "Auto-fix mode that applies
trivial fixes and stages them" contains: "auto-fix" - path:
"tests/cross-doc-deps.test.js" provides: "Tests for diffPattern filtering and
auto-fix logic" key_links: - from: "scripts/config/doc-dependencies.json" to:
"scripts/check-cross-doc-deps.js" via: "loadConfigWithRegex reads rules"
pattern: "loadConfig.\*doc-dependencies" - from:
"scripts/check-cross-doc-deps.js" to: ".husky/pre-commit" via: "pre-commit step
7 calls check-cross-doc-deps" pattern: "check-cross-doc-deps"

---

<objective>
Recalibrate cross-doc dependency rules to reduce override rate from 50% and add auto-fix mode for trivial violations.

Purpose: Cross-doc deps is the #1 overridden gate (50% of all overrides =
83/166). Most overrides happen because rules fire on insignificant changes.
Adding diffPattern filters to high-override rules and auto-fix for trivial
violations will make the gate useful instead of noise. Output: Recalibrated
doc-dependencies.json rules + auto-fix mode in check-cross-doc-deps.js + tests
</objective>

<execution_context> @~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gate-recalibration/06-RESEARCH.md
@scripts/config/doc-dependencies.json
@scripts/check-cross-doc-deps.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recalibrate doc-dependencies.json rules and add diffPattern/gitFilter</name>
  <files>scripts/config/doc-dependencies.json</files>
  <action>
  Recalibrate the 11 rules in doc-dependencies.json to reduce false triggers:

1. **Rule 1 (ROADMAP.md -> SESSION_CONTEXT.md):** Add `"checkDiff": true` and
   `"diffPattern": {"source": "Phase \\\\d|Sprint|Status.*(COMPLETE|NOT STARTED)|Current Focus|\\\\d+/\\\\d+", "flags": "i"}`.
   This is the highest-override rule -- currently fires on ANY ROADMAP.md change
   including whitespace.

2. **Rule 4 (.claude/hooks/ -> TRIGGERS.md, DEVELOPMENT.md):** Already has
   `gitFilter: "AD"` but still gets overridden. Add `"checkDiff": true` and
   `"diffPattern": {"source": "function|module\\\\.exports|require\\\\(|addEventListener", "flags": ""}`
   to only fire when hook code logic changes, not internal variable/comment
   edits.

3. **Rule 8 (functions/src/admin -> ROADMAP.md):** Add `"gitFilter": "AD"`
   (missing -- currently fires on all modifications).

4. **Rule 11 (docs/technical-debt/ -> SESSION_CONTEXT.md):** Add
   `"checkDiff": true` and
   `"diffPattern": {"source": "MASTER_DEBT|S0|S1|critical|blocker", "flags": "i"}`
   to only fire on significant debt changes, not routine JSONL appends.

Leave all other rules as-is -- they already have appropriate filters or low
override rates. </action> <verify>Run
`node scripts/check-cross-doc-deps.js --help` or a dry invocation to confirm the
config loads without errors. Verify JSON is valid with
`node -e "require('./scripts/config/doc-dependencies.json')"`. Count rules with
diffPattern -- should be at least 3 (up from 1).</verify>
<done>doc-dependencies.json has diffPattern on rules 1, 4, 11; gitFilter on rule
8; all rules load without error</done> </task>

<task type="auto">
  <name>Task 2: Add --auto-fix mode to check-cross-doc-deps.js with tests</name>
  <files>scripts/check-cross-doc-deps.js, tests/cross-doc-deps.test.js</files>
  <action>
  Add `--auto-fix` flag support to check-cross-doc-deps.js (GATE-02):

1. Parse `--auto-fix` from process.argv alongside existing flags.

2. Create an `attemptAutoFix(issue)` function that handles trivial fixes:
   - For dependent docs that just need a "Last synced" timestamp: append/update
     a comment line `<!-- Last synced: YYYY-MM-DD -->` at the end of the file.
   - For COMMAND_REFERENCE.md: log suggestion only (too complex to
     auto-generate).
   - For ROADMAP.md dependents: log suggestion only (requires human judgment).
   - Return `{ fixed: boolean, file: string, action: string }`.

3. When `--auto-fix` is active and `attemptAutoFix` succeeds:
   - Stage the fixed file with `execFileSync('git', ['add', '--', fixedFile])`.
   - Log `Auto-fixed: {dependent}` in green.
   - Remove the issue from the failure list.

4. Only remaining (unfixed) issues should cause gate failure.

5. Use `execFileSync` (not `exec`) for git staging. Import from
   `node:child_process`.

6. Use `safeWriteFile` from `scripts/lib/security-helpers.js` for file writes
   (not raw fs.writeFileSync).

For tests in tests/cross-doc-deps.test.js:

- Test that `attemptAutoFix` returns `{ fixed: true }` for a doc file with sync
  comment support.
- Test that `attemptAutoFix` returns `{ fixed: false }` for ROADMAP.md
  dependents.
- Test that diffPattern filtering correctly skips non-matching diffs.
- Use `node:test` (project convention), mock fs/git operations. </action>
  <verify>Run `node --test tests/cross-doc-deps.test.js` -- all tests pass. Run
  `node scripts/check-cross-doc-deps.js --auto-fix --help` to confirm flag is
  recognized without error.</verify> <done>--auto-fix flag works: trivial doc
  violations auto-fixed and staged, complex violations logged as suggestions,
  tests pass</done> </task>

</tasks>

<verification>
- `node -e "require('./scripts/config/doc-dependencies.json')"` exits 0
- `node --test tests/cross-doc-deps.test.js` passes
- At least 3 rules in doc-dependencies.json have diffPattern (up from 1)
- --auto-fix flag is handled without error
</verification>

<success_criteria> Cross-doc gate rules recalibrated with diffPattern/gitFilter
on the highest-override rules. Auto-fix mode implemented for trivial violations.
Tests verify both filtering and auto-fix logic. </success_criteria>

<output>
After completion, create `.planning/phases/06-gate-recalibration/06-01-SUMMARY.md`
</output>
